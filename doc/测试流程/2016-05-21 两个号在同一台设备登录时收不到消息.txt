测试原因：
A/B两个号在同一天设备上先后登录，A首先登录，然后A退出，B再登录，接收不到A发的离线消息；


注：
A号[990357]（18824582749 用手机号注册）   B号[314084]（用微信注册）


测试流程：
1.A登录，向B发送消息“测试110”；
2.B登录，接收到的消息为；

测试结果：
B在收到消息后，为了消息不重复，B端会查数据库中是否存在相同 packetID,mid，因为A发送的消息也保存在了数据库，所以B在查数据库时，检查到已经存在，就会将该条数据丢弃掉，因此客户端B就会出现收不到消息的情况；
重点原因在于，B检查数据库时的条件不完善，应该视同  where userID = ? and txtMsgID = ?